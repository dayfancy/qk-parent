<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qk.management.mapper.UserMapper">
    <insert id="insertById">
        INSERT INTO user(username,password,name,phone,email,gender,status,create_time,update_time
        <if test="deptId != null">
            ,dept_id
        </if>
        <if test="roleId != null">
            ,role_id
        </if>
        <if test="image != null and image!=''">
            ,image
        </if>
        <if test="remark != null and remark!=''">
            ,remark
        </if>
        )
        VALUES(#{username},#{password},#{name},#{phone},#{email},#{gender},#{status},#{createTime},#{updateTime}
        <if test="deptId != null">
            ,#{deptId}
        </if>
        <if test="roleId != null">
            ,#{roleId}
        </if>
        <if test="image != null and image!=''">
            ,#{image}
        </if>
        <if test="remark != null and remark!=''">
            ,#{remark}
        </if>
        )
    </insert>
    <update id="update">
        UPDATE user
        <set>
            <if test="username != null and username !=''">
                username = #{username},
            </if>
            <if test="name != null and name!=''">
                name = #{name},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="email != null and email !=''">
                email = #{email},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <if test="roleId != null">
                role_id = #{roleId},
            </if>
            <if test="image != null and image!=''">
                image = #{image},
            </if>
            <if test="remark != null and remark!=''">
                remark = #{remark},
            </if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE FROM user WHERE id IN
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectByPage" resultType="com.qk.entity.domain.user.UserDO"
            parameterType="com.qk.dto.user.UserDTO">
        SELECT
        u.*,
        d.name AS deptName,
        r.name AS roleName
        FROM
        user AS u
        LEFT JOIN dept d ON u.dept_id = d.id
        LEFT JOIN role r ON u.role_id = r.id
        <where>
            <if test="name != null and name!=''">
                AND u.name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="phone != null and phone!=''">
                AND u.phone LIKE CONCAT('%',#{phone},'%')
            </if>
            <if test="status != null">
                AND u.status = #{status}
            </if>
            <if test="deptId != null">
                AND u.dept_id = #{deptId}
            </if>
        </where>
        ORDER BY u.update_time DESC
    </select>
    <select id="selectByRole" resultType="com.qk.entity.domain.user.UserDO">
        SELECT
        u.*,
        d.name AS deptName,
        r.name AS roleName
        FROM
        user AS u
        LEFT JOIN dept d ON u.dept_id = d.id
        LEFT JOIN role r ON u.role_id = r.id
        WHERE
        r.label = #{roleLabel}
    </select>
</mapper>