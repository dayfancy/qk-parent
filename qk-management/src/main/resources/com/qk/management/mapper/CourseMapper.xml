<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qk.management.mapper.CourseMapper">
    <insert id="add">
        INSERT INTO
            Course(name,subject,price,target,create_time,update_time
                 <if test="description != null and description !=''" >
                  ,description
                 </if>
        )
        VALUES
            (#{name},#{subject},#{price},#{target},#{createTime},#{updateTime}
        <if test="description != null and description !=''" >
            ,#{description}
        </if>
        )
    </insert>
    <update id="update">
        UPDATE Course
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="subject != null">
                subject = #{subject},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="target != null">
                target = #{target},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="createTime != null">
                create_time = #{createTime}
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectByPage" resultType="com.qk.entity.Course">
        SELECT * FROM Course
        <where>
            <if test="name != null and name!=''">
                AND name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="subject != null">
                AND subject = #{subject}
            </if>
            <if test="target != null">
                AND target = #{target}
            </if>
        </where>
        ORDER BY update_time DESC
    </select>
</mapper>